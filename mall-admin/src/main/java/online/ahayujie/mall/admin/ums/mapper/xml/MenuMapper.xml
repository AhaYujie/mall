<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="online.ahayujie.mall.admin.ums.mapper.MenuMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="online.ahayujie.mall.admin.ums.bean.model.Menu">
    <result column="id" property="id" />
    <result column="update_time" property="updateTime" />
    <result column="create_time" property="createTime" />
        <result column="is_deleted" property="isDeleted" />
        <result column="parent_id" property="parentId" />
        <result column="title" property="title" />
        <result column="level" property="level" />
        <result column="sort" property="sort" />
        <result column="name" property="name" />
        <result column="icon" property="icon" />
        <result column="hidden" property="hidden" />
    </resultMap>

    <select id="selectByParentId" resultMap="BaseResultMap">
        select
        id, update_time, create_time, is_deleted, parent_id, title, level, sort, name, icon, hidden
        from ums_menu
        where parent_id = #{parentId}
        order by sort desc
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        select
        id, update_time, create_time, is_deleted, parent_id, title, level, sort, name, icon, hidden
        from ums_menu
    </select>

    <select id="selectAllByParentId" resultMap="BaseResultMap">
        select
        id, update_time, create_time, is_deleted, parent_id, title, level, sort, name, icon, hidden
        from ums_menu
        where parent_id = #{parentId}
    </select>

</mapper>
